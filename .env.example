# Re-analysing the published predictions needs only HF_TOKEN (LLM-AggreFact is a
# gated dataset: accept its terms on the Hugging Face page first).
# The API keys are needed only to re-run the models.
HF_TOKEN=
TYPESAFE_API_KEY=
OPENAI_API_KEY=
